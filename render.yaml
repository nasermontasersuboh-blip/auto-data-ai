services:
  - type: web
    name: auto-data-ai
    env: python
    plan: free
    rootDir: src
    buildCommand: pip install -r ../requirements.txt
    startCommand: uvicorn app:app --host 0.0.0.0 --port $PORT
